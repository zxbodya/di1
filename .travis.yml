language: node_js
sudo: false
node_js:
  - '8'
  - '10'

cache:
  directories:
    - node_modules

install:
  - npm install

script:
  - npm run lint && npm run cover

after_success:
  - ./node_modules/.bin/codecov
